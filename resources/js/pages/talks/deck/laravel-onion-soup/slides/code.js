// Shared config for the animated <Code> / <CodeClass> blocks across slides.
export const codeTheme = 'vesper';
export const codeOptions = {
    duration: 600,
    stagger: 0.15,
    containerStyle: false,
};
